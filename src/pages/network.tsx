import React from 'react';
import Header from '../sections/header';

function Network() {
  return (
    <section>
      <Header 
        currentPage="network"
      />

      <section>
        BODY
      </section>

      <section>
        FOOTER
      </section>
    </section>
  );
}

export default Network;
